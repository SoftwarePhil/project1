1. create a generic store ets mdoule(ItemStore, RentalStore) and use github/danielberkompas/immortal
2. a user needs a list of their items that they have up for rent
3. make it so duplicate items cannot be added (maybe make it so that items also have unique ids just like rentals)
4 in base change location -> city